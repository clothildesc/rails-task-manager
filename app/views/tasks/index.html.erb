<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <div>
    <input type="checkbox" id="<% task.title %>" name="<% task.title %>" checked=<% task.completed %>/>
    <label for="<% task.title %>"><%= link_to task.title, task_path(task) %> • <%= link_to "Delete", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></label>
  </div>
<% end %>

<p><%= link_to "+Add a new task", new_task_path %></p>
